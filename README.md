# c-simple
Statically typed, type inferred, garbage-collected programming language that transpiles to C. 100% of C's functionality with much less headaches.

Running `./bin/run <file.csimple>` now compiles the source to a temporary C program, builds it, and executes the result. Pass `--emit-c` if you want the generated C source instead.
